PORT=3000
# Required in production: any long random string. The app must send it back
# as the "x-api-key" header, otherwise anyone who finds this server's URL
# could use it to convert videos on your bandwidth/compute.
API_KEY=change-me-to-a-long-random-string
